/* Declares a function that takes two ints and returns an int:
 * NOTE: The compiler needs this information to generate machine code in order
 *       to call this function, and will not look ahead to find it. */
int add(int, int);

/* Defines shared constants: */
#define ONE 1
#define TWO 2
