/* Defines a function that takes two ints and returns an int: */
int add(int x, int y) {
    /* NOTE: The beginning of a function follows a left curly brace, and is
     *       thus a valid place to declare local variables. */
    int z = x + y;

    return z;
}
